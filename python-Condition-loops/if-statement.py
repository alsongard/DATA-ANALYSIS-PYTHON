"""
    branches and condition - branches enables the execution of statements based on a condition

    if , if...else, if...elif..else, 
    while and do..while loops for iteration
    for loop for 
"""

myNumber = input("Enter any number : \n")
if int(myNumber) % 2 == 0:
    print("the number {} is an even number".format(myNumber))